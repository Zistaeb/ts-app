const numbers: ReadonlyArray<number> = [1,2,3,4,5,6,];
//numbers.push(7);                //Inserta al final del array
//numbers.pop();                  //Elimina el ultimo elemento del array
//numbers.unshift(1);             // Inserta al inicio del array
numbers.filter(() => {});       //Activo como se ve los metodos que no mutarian el estado original
numbers.reduce(() => 0);
numbers.map(() => {});
